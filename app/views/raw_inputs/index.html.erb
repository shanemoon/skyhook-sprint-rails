<h1>Listing raw_inputs</h1>

<table>
  <tr>
    <th>Input</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @raw_inputs.each do |raw_input| %>
  <tr>
    <td><%= raw_input.input %></td>
    <td><%= link_to 'Show', raw_input %></td>
    <td><%= link_to 'Edit', edit_raw_input_path(raw_input) %></td>
    <td><%= link_to 'Destroy', raw_input, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Raw input', new_raw_input_path %>
